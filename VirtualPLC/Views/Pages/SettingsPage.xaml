<Page
    x:Class="VirtualPLC.Views.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="clr-namespace:VirtualPLC.Helpers"
    xmlns:local="clr-namespace:VirtualPLC.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="SettingsPage"
    d:DataContext="{d:DesignInstance local:SettingsPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">
    <Page.Resources>
        <helpers:EnumToBooleanConverter x:Key="EnumToBooleanConverter" />
    </Page.Resources>

    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="IP" Margin="10 0 10 0"/>
            <TextBox Text="{Binding ViewModel.IP, Mode=TwoWay}"
             FontSize="24"
             FontWeight="Bold"
             Foreground="White"
             BorderThickness="2"
             Padding="12,6"
             Width="180"
             HorizontalContentAlignment="Center"
             VerticalContentAlignment="Center"
             />
            <TextBlock Text="Port" Margin="10 0 10 0"/>
            <TextBox Text="{Binding ViewModel.Port, Mode=TwoWay}"
             FontSize="24"
             FontWeight="Bold"
             Foreground="White"
             BorderThickness="2"
             Padding="12,6"
             Width="180"
             HorizontalContentAlignment="Center"
             VerticalContentAlignment="Center"
             />
        </StackPanel>
        <ui:Button Content="Save" Command="{Binding ViewModel.SaveCommand}"/>
        <TextBlock
            Margin="0,24,0,0"
            FontSize="20"
            FontWeight="Medium"
            Text="About VirtualPLC" />
        <TextBlock Margin="0,12,0,0" Text="{Binding ViewModel.AppVersion, Mode=OneWay}" />
    </StackPanel>
</Page>
